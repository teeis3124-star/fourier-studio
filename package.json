{
  "name": "fourier-studio",
  "version": "0.4.0",
  "private": true,
  "description": "Interactive 1D and 2D Fourier studio with drawing, complex epicycles, spectra and live reconstruction.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.mjs",
    "test": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
