{
  "name": "fourier-studio",
  "version": "0.7.0",
  "private": true,
  "description": "Interactive Fourier studio with Fourier series, real waveform FFT analysis, WAV/CSV import, spectra, reconstruction and complex epicycles.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.mjs",
    "test": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
