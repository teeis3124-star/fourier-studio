{
  "name": "fourier-studio",
  "version": "0.6.0",
  "private": true,
  "description": "Interactive Fourier studio with 1D/2D analysis, complex epicycles, contour import, exports and reconstruction metrics.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.mjs",
    "test": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
