{
  "name": "fourier-studio",
  "version": "0.5.0",
  "private": true,
  "description": "Interactive Fourier studio with 1D/2D drawing, contour extraction, complex epicycles, spectra and live reconstruction.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.mjs",
    "test": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
